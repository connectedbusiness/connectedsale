/**
 * @author Connected Business
 */
define([
  'model/base'
], function(BaseModel) {
  var InvoiceDetailModel = BaseModel.extend({
  	initialize: function(){
  		
    }
  });
  return InvoiceDetailModel;
});