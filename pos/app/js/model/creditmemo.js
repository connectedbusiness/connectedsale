/**
 * Connected Business | 06-1-2012
 */
define([
  'model/base'
], function(BaseModel) {
  var CreditMemoModel = BaseModel.extend({  	
  	
  	// model: function(model) {
  		// this.model = model;
  	// },
//   	
  	// toJSON: function() {
    	// return this.model.toJSON(); // where model is the collection of invoice detail to be converted to CM
 	// }
  
  });
  return CreditMemoModel;
});